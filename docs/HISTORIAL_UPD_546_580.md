# HISTORIAL UPD-546 a UPD-580
# Archivado desde CLAUDE.md el 16-sep-2026

| Código | Fecha | Resp. | Descripción |
|---|---|---|---|
| UPD-546 | 26-ago-2026 | Armando | Tabla "Saldos a Favor" del Reporte Dirección — 2 columnas nuevas "Saldo Previo" y "Saldo Posterior" por depósito/bono, para ver de un vistazo cuánto tenía el cliente antes del abono y cuánto le queda después. `api/reporte_direccion.php` `accion=ventas_cobranza`: como el saldo de un cliente no arranca en `$desde` del período, se trae el HISTÓRICO COMPLETO de `clientes_saldo_favor` (los 4 tipos: deposito/aplicacion/ajuste/referido, no solo lo mostrado en la tabla) de los clientes que aparecen en el período, se calcula el saldo corrido en PHP (ordenado por fecha/id) y se le pega `saldo_previo`/`saldo_posterior` a cada fila de depósito/referido que ya se mostraba. `app/modulos/reporte_direccion.php` agrega las 2 columnas a la tabla. Verificado a mano con un cliente real de 14 movimientos mezclados (depósitos + aplicaciones desde jun-2026): la secuencia calculada cuadra exacto (ej. depósito de $60,320 el 17-jul: previo $1,332.28 → posterior $61,652.28). `php -l` OK + `node --check` del `<script>` embebido. Sin prueba visual en navegador — pendiente que Armando lo confirme viéndolo en vivo. |

| UPD-547 | 26-ago-2026 | Armando | **Umbral de $10 para acreditar excedente de pago a Saldo a Favor.** Regla nueva de Armando: si un cliente tiene, por ejemplo, $994 pendientes y deposita $1,000, el excedente de $6 ya NO se acredita como depósito de saldo a favor — se registra el pago completo tal cual ($1,000 aplicados a la orden) y el residuo queda absorbido por ahora (pendiente que Armando defina más adelante cómo tratar esos pesos/centavos). Antes (A-10, UPD-531) CUALQUIER excedente >$0.01 generaba un depósito formal en `clientes_saldo_favor`. `api/finanzas.php` `accion=registrar_pago`: el corte pasó de `> 0.01` a `>= $10.00` (constante `$UMBRAL_EXCEDENTE_SALDO_FAVOR`) tanto para `$monto_aplicar` como para `$depositar_favor` — un solo endpoint, usado por Cobranza y por VoBo (`finanzas_vobo.php` llama el mismo `accion=registrar_pago`), así que el fix cubre ambas pantallas con un cambio. El mensaje de "excedente abonado a saldo a favor" en ambos frontends ya solo depende de `data.excedente` (queda `null` cuando el excedente es <$10), sin tocar frontend. **No se tocó** el patrón similar de `api/correcciones.php` (BLV-5, excedente cuando una corrección baja el total por debajo de lo ya cobrado) — dispara en un escenario distinto (corrección de dir_admin, no depósito del cliente) y Armando no lo mencionó; queda como posible ajuste aparte si también lo quiere con el mismo umbral. Verificado con casos reales de producción (ej. COT-0976, $1.97 pendiente). `php -l` OK. |

| UPD-548 | 27-ago-2026 | Armando | **Columna "Razón" en la tabla "Retrabajo del período" de Reporte Dirección (UPD-542) + aclaración de alcance.** Armando preguntó si se podía agregar la razón del retrabajo a esa tabla, y si ya incluía el retrabajo por errores de producción (piso). Aclarado: NO — esa tabla solo cuenta retrabajo **comercial** (`cotizaciones.es_retrabajo=1`, nace con folio/orden propia); el retrabajo de **piso** (`api/reproceso.php`, marca `piezas.es_retrabajo=1` + `razon_retrabajo` por pieza, reprocesa dentro de la MISMA orden sin folio nuevo) vive aparte en el módulo `?m=retrabajo` — decisión explícita de Armando de NO fusionarlos en esta tabla. El retrabajo comercial nunca capturaba motivo alguno (solo un checkbox al crear la cotización) — se agregó de cero: (1) BD aditiva `cotizaciones.motivo_retrabajo VARCHAR(255) NULL` (checkpoint: tag git `pre-motivo-retrabajo-2026-08-27` + backup `_backups/pre_motivo_retrabajo_cotizaciones_20260827.sql`); (2) `app/modulos/cotizacion.php` — textarea "Motivo del retrabajo *" en el panel rojo de retrabajo (`#rtMotivo`), obligatorio antes de guardar; (3) `api/cotizaciones.php` (`crear`) — valida que venga no vacío cuando `es_retrabajo=1` y lo guarda en el INSERT; (4) `api/reporte_direccion.php` (`accion=ventas_cobranza`) — agrega `motivo_retrabajo` a la fila de la tabla 2; (5) `app/modulos/reporte_direccion.php` — columna nueva "Razón" en esa tabla (colspan del detalle de pagos ajustado 5→6). Los retrabajos ya existentes (S-593/610/659/665) quedan con motivo `NULL` (se ven como "—") — no hay dato histórico que rescatar, solo aplica hacia adelante. Verificado con dry-run real en BD (`BEGIN`/`ROLLBACK`): INSERT con `motivo_retrabajo` OK, 0 filas de prueba quedaron en producción; confirmado que los 4 retrabajos reales existentes traen `NULL` como se esperaba. `php -l` en los 4 archivos + `node --check` de los `<script>` embebidos de ambos módulos frontend, todo OK. Sin prueba visual en navegador — pendiente que Armando confirme creando una cotización de retrabajo real y viendo la columna en Reporte Dirección. |

| UPD-549 | 27-ago-2026 | Armando | **Rediseño de "Resumen" (`?m=resumen`, página de entrada del dashboard) — de tablero solo-operativo a bienvenida corporativa con KPIs.** Motivo de Armando: nadie usaba esa página porque no era funcional (solo mostraba conteo de piezas por estatus + tabla de órdenes activas). Cambios: (1) `app/modulos/resumen.php` — banda superior navy (`#0f172a`, mismo tono del topbar) con saludo dinámico por hora + fecha en español, usando Syncopate (la misma fuente del logo "APEX GLASS") como firma visual — sin emojis, iconografía SVG vía `api/helpers/icons.php`; (2) fila de KPIs **Comercial** (Ventas del mes, Cobrado del mes, Por cobrar, Pendientes de VoBo) — visible **solo** a roles con visión financiera (dueño/dir_admin/director/administración/desarrollo, mismo criterio que Reporte Dirección), oculta a comercial/jefe_piso; reutiliza exactamente el mismo WHERE que la query canónica de Reporte Dirección (`c.es_retrabajo = 0`, UPD-510/512/541) para no reintroducir ese bug con una definición de "ventas" distinta; (3) fila de KPIs **Producción** (Órdenes activas, Piezas en proceso, Órdenes con retraso, Piezas terminadas hoy) — visible a todos los roles que entran a Resumen; (4) tabla de Órdenes Activas y Actividad Reciente se quedan igual, debajo de las 2 filas nuevas, sin tocar su lógica. **Bono de paso:** el badge de "vencidas" del sidebar usaba un conteo hecho en JS solo sobre las 15 órdenes de la página cargada (nunca todas las activas) — pendiente documentado en sección 12 desde hace semanas ("Badge órdenes vencidas global"); ahora usa el conteo real server-side (`operativo.retraso`, cuenta TODAS las órdenes activas con `fecha_entrega < hoy` y al menos una pieza sin terminar), así que ese pendiente queda resuelto como efecto colateral. `api/dashboard.php` agrega los bloques `operativo` (siempre) y `comercial` (null si el rol no califica) a la respuesta JSON existente, sin tocar los campos que ya consumían otros módulos. Las 6 queries nuevas se corrieron en modo solo-lectura contra producción antes de integrarlas (42 órdenes activas, $903,095.65 ventas del mes, $843,091.75 cobrado, $69,007.72 por cobrar, 4 pendientes de VoBo, 1 orden con retraso real — todo cuadra). `php -l` + `node --check` del `<script>` embebido en ambos archivos, OK. **Pendiente:** prueba visual en navegador — no hay Chrome DevTools/Playwright MCP en esta sesión; falta que Armando confirme que la banda de bienvenida, los KPIs y el ocultamiento de la fila Comercial para roles sin `ver_reportes` se ven como se espera. |

| UPD-550 | 27-ago-2026 | Mando | Auditoría 25-ago Sprint P0 (dinero/fiscal): CFDI ya timbra conceptos reconstruidos en servidor con descuento de referido y servicios (`facturapi.php`); `distribuir_flete` ya acumula costo y genera póliza (`ordenes_compra.php`); de paso, fix de `flete_tipo` con valor de ENUM inválido que truena en producción. Probado en BD con rollback, falta prueba visual. |
| UPD-551 | 27-ago-2026 | Mando | Auditoría 25-ago Sprint P1: reversa de entrega solo Logística+motivo+bitácora (`rutas.php` BLO-04), escaneo de piezas transaccional y exige 'terminado' (BLO-05), retry en colisión de código de cliente (`clientes.php`), salida exige orden activa (`salidas.php`). BLO-08 (portal) queda pendiente. Probado en BD con rollback. |

| UPD-552 | 28-ago-2026 | Armando | Fix: los resaques de Maquila (UPD-533) no aparecían en la Orden de Producción impresa (`app/imprimir_orden.php?id=1440`, reportado por Armando). Causa: UPD-533 cableó el cobro y la captura de resaques (`api/maquila.php`, `app/modulos/maquila.php`/`maquila_precios.php`, `piezas.resaques`) pero nunca tocó este archivo de impresión — el arreglo de "Servicios" por partida (rama `$esMaquila`) solo listaba Corte/Canteado-Filo Muerto/Taladro/Templado. Verificado en BD que el dato sí estaba guardado (cotización 1440/orden MA-S-701, partidas 1/3/5 con 2 resaques c/u). Agregada la línea `if ((int)($p['resaques'] ?? 0) > 0) $servicios[] = 'Resaque (' . $p['resaques'] . ')';` en `app/imprimir_orden.php`. `php -l` OK. Sin prueba visual en navegador — pendiente que Armando confirme viendo MA-S-701 impresa. |

| UPD-553 | 28-ago-2026 | Armando | **Estación Canteado: registro directo a Terminado cuando la pieza no necesita nada más.** Motivado por el caso de S-674 (espejo, sin templado/taladro/resaques): Armando notó que "Terminado" para esas piezas era puro papeleo — nadie hace nada físico ahí, solo alguien tenía que ir a escanear la pieza otra vez en la estación Terminado sin agregar valor, ya que el equipo de Canteado ya la dejó lista. Criterio (no amarrado al nombre "espejo", sino a la condición real, confirmado con Armando): pieza de Suministro (`p.tipo !== 'maquila'`) sin templado (`requiere_templado=0`) y sin taladro/resaques (`tp=ta=resaques=0`) — mismo cálculo que ya usaba `nextEstatus()` para saber que el siguiente paso lógico tras canteado es terminado directo. Cambio solo en `app/operador.php`: en la estación Canteado, cuando la pieza escaneada cumple esa condición, el botón pasa a "▶ Registrar: Canteado y Terminado" y la nueva función `doUpdateCanteadoTerminado()` hace las 2 llamadas a `api/actualizar_estatus.php` en secuencia (primero `canteado`, si sale bien encadena `terminado`) — sigue quedando canteado en el historial (trazabilidad de quién/cuándo), solo que ya no espera un segundo escaneo de otra persona. Sin cambios de backend — `actualizar_estatus.php` ya aceptaba `canteado→terminado` directo para piezas sin templado sin necesitar `omision:1` (confirmado leyendo `FLUJO_PREVIO['terminado']`). Si la 2ª llamada (terminado) falla, la pieza queda registrada en canteado y se avisa por toast para cerrarla manual — no se pierde nada. Checkpoint: tag git `pre-canteado-terminado-directo-2026-08-28`. `php -l` OK. Sin prueba visual en navegador (no hay Chrome DevTools/Playwright MCP en esta sesión) — pendiente que Armando/piso lo confirmen escaneando una pieza de espejo real en Canteado. |

| UPD-554 | 31-ago-2026 | Armando | **Venta anticipada de lámina completa (sin cortar), con reserva contra compra futura.** Motivado por un caso real: Armando va a comprar 66 láminas de un tipo, pero 3 de esas ya tienen comprador desde antes de que llegue la compra. Antes no había forma de vender una lámina completa (siempre se cotizaba por pieza cortada a medida) ni de comprometer una venta contra stock que todavía no existe físicamente. **BD (aditivo, con backup + tag `pre-venta-lamina-completa-2026-08-31`):** `cotizaciones_partidas` +columna `lamina_id` (liga la partida a la lámina exacta del catálogo `laminas` que se vende completa, sin cortar); tabla nueva `laminas_reservas` (`lamina_id`, `cantidad`, `cantidad_cumplida`, `cotizacion_partida_id`, `folio`, `estado` activa/cumplida/cancelada) — **no se liga a una OC en particular a propósito**: en cuanto llega CUALQUIER compra (por OC o manual) de esa misma lámina de catálogo, se liquida sola por antigüedad (FIFO), sin que Armando tenga que ir a ligar nada. **Helper nuevo `api/helpers/laminas_reservas.php`** con 3 funciones: `laminasCrearReservaVenta()` (crea la reserva al VoBo e intenta liquidarla contra el stock que ya exista), `laminasLiquidarReservas()` (se llama cada vez que entra una compra nueva — resuelve reservas activas más viejas primero, generando el movimiento de salida en `inventario_movimientos` solo por lo que realmente se cubre; soporta liquidación parcial en varias entregas), `laminasRevertirReservaPorPartida()` (al cancelar la orden, regresa a stock físico lo que ya se hubiera liquidado con un movimiento compensatorio — nunca borra el historial). **Conectado en 4 puntos:** (1) `api/finanzas.php` VoBo — por cada partida con `lamina_id`, crea la reserva; (2) `api/cotizaciones.php` y `api/admin_ordenes.php` acción cancelar — revierte la reserva de esa cotización; (3) `api/inventario.php` `registrar_compra` (ahora transaccional) — liquida reservas de esa lámina tras la compra manual; (4) `api/ordenes_compra.php` recepción de OC — liquida reservas tras cada entrega recibida de una partida tipo lámina. **Cotización (`app/modulos/cotizacion.php`):** checkbox "Vender lámina completa (descuenta stock)" por partida — al marcarlo aparece un selector de la lámina de stock (tipo/espesor/medida, catálogo `laminas`) que autocompleta ancho×alto de la partida con la medida exacta; el precio no cambia (sigue siendo `precio_m2` del catálogo Cristales × m² × cantidad, antes de IVA, como cualquier partida). **Inventario (`app/modulos/inventario.php` + `api/laminas.php` `accion=stock`):** nuevo campo `reservado`/`disponible` por lámina — la tabla de Stock muestra "N reservada(s) — venta anticipada" en ámbar bajo el conteo de stock cuando aplica, para que nadie cuente ni corte por error una lámina ya comprometida. **Fuera de alcance de este UPD** (mismo criterio que el pendiente C-6 ya documentado): el Optimizador de Corte y Sesión de Corte no descuentan lo reservado de su propio cálculo de disponibilidad — si hace falta cerrar ese hueco, se hace aparte. Probado con dry-run real en BD (`BEGIN`/`ROLLBACK`, sin tocar el flujo web): venta sin stock → reserva activa 0 cumplida; llega compra → liquida y stock libre correcto; 2 reservas de distinta antigüedad sobre la misma lámina con una compra que solo alcanza para la primera → confirma orden FIFO y liquidación parcial de la segunda; segunda entrega completa la reserva parcial; reversión por cancelación regresa el stock exacto — las 3 corridas terminaron con 0 filas de prueba en producción. `php -l` limpio en los 9 archivos tocados/creados + `node --check` del `<script>` embebido en ambos módulos frontend. **Sin prueba visual en navegador** (no hay Chrome DevTools/Playwright MCP en esta sesión) — pendiente que Armando confirme creando una cotización real con "Vender lámina completa" marcado y viendo el aviso de reservado en Inventario. |

| UPD-555 | 02-sep-2026 | Mando | Fix reporte #19: en `app/imprimir_salida.php` el QR de "escanear al cargar" solo se imprimía si el tipo de entrega calculado AL ABRIR la página era 'chofer' — en una orden con salida parcial previa de otro tipo (ej. S-554: recolección 14-ago, luego chofer 31-ago), el cálculo inicial tomaba el tipo de la salida anterior y el QR nunca aparecía, aunque en pantalla se seleccionara "Chofer" (el botón solo cambiaba texto, no mostraba el QR). Ahora el bloque del QR siempre se genera y el botón "Chofer/Recolección" lo muestra/oculta al instante. `php -l` OK. Sin prueba visual en navegador — pendiente confirmar imprimiendo una salida parcial real. |

| UPD-556 | 02-sep-2026 | Mando | Fix reporte #17: en `app/imprimir_croquis.php` (PDF de croquis), un resaque colocado exactamente pegado a un borde (0mm de distancia — común en resaques para clips) hacía que el código, para "evitar mostrar 0mm", calculara la cota desde el borde OPUESTO — resultando en una cota gigante y falsa cruzando toda la pieza (ej. "1160 mm" o "1900 mm") que tapaba visualmente la cota correcta del otro eje (ej. "250 mm"). Quitada esa regla — ahora un resaque pegado al borde simplemente muestra "0 mm", sin cruzar la pieza. Verificado recreando el cálculo con los datos reales de la partida del reporte (1160×1900mm, 4 resaques). `php -l` OK. **Confirmado visualmente por Mando 02-sep-2026** (screenshot real de COT-1533/S-716, GERARDO GAUNA): ya no hay cota falsa cruzando la pieza, los 4 resaques muestran "0 mm" en el lado que están pegados al borde. |

| UPD-557 | 02-sep-2026 | Mando | **Auditoría 25-ago, resto de Franja 1 (de "Correcciones" para abajo, a petición explícita).** BLV-6: `api/correcciones.php` ya clampa descuento 0-100% y descarta ancho/alto/precio negativos (mismo candado S2-05 que ya tenía cotizaciones.php); de paso, fix de que `cantidad` se guardaba cruda (podía quedar <1) aunque el total ya se calculaba con mínimo 1 — ahora usan el mismo mínimo. BLO-03: `api/ordenes_compra.php` `cambiar_estado` ya no deja forzar "cerrada" con material sin recibir ni "pagada" sin que el saldo esté cubierto por pagos reales (reabrir hacia atrás no se tocó, sigue igual). BLO-06: `api/admin_ordenes.php` `corregir_estatus` revivía una orden cancelada a "activa" a ciegas (sin pasar por el candado que sí tiene `accion=restaurar` de re-vincular la cotización) — ahora se rechaza y pide restaurar primero. BLO-11: `api/campanas.php` ya no inserta destinatarios duplicados por teléfono al armar una campaña (mismo número en distinto formato, o repetido entre cliente/prospecto) — confirmado con un caso real en BD (mismo teléfono en 2 clientes distintos) que hubiera duplicado el envío. Los 4 probados con datos reales dentro de transacciones con `ROLLBACK` garantizado, uno por uno con confirmación del usuario antes de avanzar al siguiente. `php -l` limpio en los 4 archivos. **BLO-07 (Sesión de Corte sin reversa de stock) se deja pendiente a propósito** — el más riesgoso de los 6: revertir una sesión confirmada implica decidir qué hacer con piezas que ya avanzaron en producción y con el bono de pedacería ya calculado; usuario decidió documentarlo para sesión dedicada en vez de resolverlo hoy. Con esto queda cerrada toda la Franja 1 de la auditoría del 25-ago excepto BLO-07 y BLO-08 (portal por nombre, ya documentado desde antes, esperando decisión de backfill de 216 órdenes). |

| UPD-558 | 02-sep-2026 | Mando | **Auditoría 25-ago, hallazgos que solo estaban en el detalle de `bl_auditoria_operaciones.md` (no en el resumen consolidado ya cerrado en UPD-557).** BLO-15: `api/inventario.php` `registrar_uso` (descuento manual de stock/mermas) exigía solo `ver_inventario` (que `comercial` también tiene) — ahora exige rol de escritura real (dir_admin/administracion/dueno/desarrollo) y la fecha se fuerza a hoy server-side (ya no se podía registrar con fecha atrasada para esconderla en otro período de costo). BLO-14: `api/actualizar_estatus.php` — el WhatsApp automático "orden lista" tenía check-then-set no atómico (dos escaneos casi simultáneos de las últimas 2 piezas de una orden podían mandarlo duplicado); ahora usa claim atómico antes de enviar, con liberación del candado si Meta rechaza el envío o el teléfono es inválido (mismo comportamiento de reintento de antes). BLO-10: mismo patrón en `api/rutas_lib.php` (`enviarAvisosInicioRuta`, avisos de inicio de ruta) y `api/salidas.php` (arranque automático de ruta por escaneo, que además leía `ruta_estado` con dato potencialmente viejo) — ambos ya usan el mismo claim atómico que ya tenía el botón manual "Iniciar Ruta". Los 3 puntos probados con datos reales (una orden, una ruta real) dentro de transacciones con `ROLLBACK` garantizado. `php -l` limpio en los 4 archivos. **Quedan sin tocar, documentados aparte:** BLO-12 (OC de flete pura se autocompleta sin capitalizar el costo — dinero, media) y BLO-13 (rechazo de piezas en puerta sin flujo financiero — requiere más diseño, no es solo un candado). |

| UPD-560 | 02-sep-2026 | Armando | **Fix: usuarios reportaban que el sistema "los sacaba" o "no los dejaba entrar" de forma intermitente, todo el día.** Investigación de logs (firewall, fail2ban, PHP-FPM, Apache, journalctl) descartó ataque/caída/DoS — 0 bans relevantes, sin OOM, sin errores de aplicación. El reinicio completo del VPS de hoy 20:31 UTC fue solicitado por Armando (no infraestructura) y no fue la causa: las sesiones viven en disco persistente (`/home/apexglass2025/tmp/sessions/`), sobrevivieron el reinicio sin problema. Causa real encontrada en `access_log`: ráfagas reales de `401 Sesión requerida` en `campanas.php`/`reportes.php`/`notificaciones.php`/`mensajes.php`/`autorizaciones.php` (hasta 87 en 1 minuto) — `session.gc_maxlifetime` estaba en `1440` segundos (24 min, el default de PHP, nunca ajustado a propósito) en `/etc/opt/remi/php84/php.d/zzz-apex.glass.ini`. Con jornadas donde una pestaña queda abierta sin interacción >24 min (llamada, ir a piso, junta), el servidor podía recolectar el archivo de sesión de fondo; el siguiente poll en esa pestaña recibía 401 en silencio, y si el usuario intentaba guardar algo, el token CSRF de esa pestaña ya no coincidía con la sesión nueva (mismo síntoma ya confirmado y documentado el 20-ago, que se resolvía con F5). Subido a `session.gc_maxlifetime = 28800` (8 horas) en ambos bloques `[HOST=apex.glass]`/`[HOST=www.apex.glass]` del `.ini`; `php -l` OK, `systemctl reload php84-php-fpm` (sin caída), verificado el valor efectivo en vivo con una petición HTTP real (`ini_get()` devolvió 28800 desde apex.glass). Backup del `.ini` original en `/root/backup_zzz-apex.glass.ini.<timestamp>`. Sin código tocado — cambio de configuración únicamente. |
| UPD-561 | 02-sep-2026 | Mando | **Reporte "se batalla mucho para usar el sistema" — causa real: el VPS se congeló por completo ~5.5 horas (14:59 a 20:31), sin swap configurado.** Investigado con Mando en consola root. `mysql_error.log` mostró que MariaDB nunca hizo shutdown ordenado — el último registro antes del hueco fue un timeout normal a las 14:59:16, y el siguiente fue ya el arranque con "crash recovery" (816 páginas) al reiniciarse a las 20:31 — la máquina completa quedó sin responder ese rato, no fue un servicio individual cayéndose. Mismo patrón exacto ya había ocurrido el 12-ago-2026 (segunda vez en 3 semanas). No se encontró OOM-killer ni ataque en los logs revisados — causa raíz del pico de memoria queda sin identificar. Fix aplicado (mitigación, no cura): **swap de 4GB creado y activado** (`/swapfile`, `mkswap`+`swapon`, agregado a `/etc/fstab` para persistir tras reinicio, `vm.swappiness=10` en `sysctl.conf`) — antes el VPS tenía `Swap: 0B`, así que cualquier pico de RAM no tenía colchón y podía congelar el sistema entero en vez de solo ir más lento. Verificado con `free -h` (`Swap: 4.0Gi`, 0 usado) y `tail /etc/fstab` (línea agregada sin tocar el resto). Sin cambio de código — 100% configuración de sistema operativo, ejecutado por Mando como root. **Pendiente:** si el sistema se vuelve a sentir lento antes de congelarse, correr `top`/`htop` en el momento para atrapar qué proceso está disparando el consumo de RAM — el swap es red de seguridad, no explica la causa original. **CORRECCIÓN (UPD-563, 03-sep-2026): el diagnóstico de este UPD está mal — el VPS NO se congeló.** Auditoría más profunda (root, con acceso a `historial_estatus`) encontró escrituras de base de datos continuas durante toda la ventana 08:56–14:59, sin ningún hueco largo. El hueco visto en `mysql_error.log` era simplemente ausencia de errores (un log de errores no escribe nada cuando no hay errores — no es evidencia de caída) y el "crash recovery" correspondía al reinicio de las 14:31 que Armando pidió a propósito, no a un congelamiento previo. El swap de 4GB se queda (buena práctica, no hace daño), pero no hay una causa de RAM pendiente de identificar — ese pendiente queda cerrado como falso positivo. La causa real de "se batalla para usar el sistema" ese día fue la caducidad de sesión a los ~24 min (`session.gc_maxlifetime`, ya corregida esa misma tarde por este mismo UPD) combinada, desde la noche, con el hallazgo de UPD-563 (Cloudflare ocultando la IP real de todos los usuarios). |
| UPD-562 | 02-sep-2026 | Armando | **Diagnóstico: SSH sin VPN se corta durante el intercambio de llaves — confirmado bloqueo de puerto 22 en la ruta de Armando (Telmex/router), no DNS ni servidor. Puerto 2222 agregado como workaround.** Seguimiento del hallazgo de `project_telmex_dns_desconexiones.md` (misma sesión, más temprano): Armando pasó el log real de `ssh -vvv root@82.29.197.33` sin VPN — el TCP conecta, se intercambian banners SSH y KEXINIT completo en ambos sentidos, y se corta justo cuando el cliente ya mandó el paquete ECDH init y espera la respuesta del servidor ("Connection closed by 82.29.197.33 port 22"). Verificado en el servidor con `journalctl -u sshd` (día completo): **cero rastro** de la IP de Armando tocando sshd — ni un intento fallido, ni timeout — a diferencia de 2 bots reales que sí quedan logueados aunque se corten en una etapa aún más temprana (identificación). Como conectar por IP directa no usa DNS ("hostname 82.29.197.33 is address", confirmado en su propio log), y el servidor no ve nada, la conclusión es que **algo en la ruta de Armando (router o Telmex) intercepta/simula el tráfico del puerto 22 y lo corta apenas detecta que es SSH real** — la conexión que su cliente "ve" avanzar nunca llega a este servidor. Coincide con el hallazgo previo de la misma sesión: 443 (HTTPS) sí pasa bien por el mismo bloque de Telmex, 22 no. **Fix/diagnóstico aplicado (autorizado por Armando):** `Port 2222` agregado en archivo nuevo `/etc/ssh/sshd_config.d/02-manual-port2222.conf` (separado de `01-custom.conf` a propósito — ese lo regenera AdminBolt desde su plantilla mustache y solo escribe `Port 22`, así que un segundo `Port` ahí se hubiera perdido en el próximo cambio desde el panel); `sshd -t` OK, `systemctl restart sshd` sin caída, confirmado escuchando en 22 y 2222 (`ss -tlnp`). Regla de firewall para 2222 agregada en runtime (`nft add rule ip filter INPUT tcp dport 2222 accept`) sobre la tabla real activa (`/etc/sysconfig/nftables.conf`, cargada por `nftables.service` — **no** la de `/etc/nftables/main.nft`, que es solo un archivo de ejemplo sin usar). Nota importante: la regla de firewall es **solo en runtime, no persiste un reinicio** — y `/etc/sysconfig/nftables.conf` trae la advertencia "auto-generado, no editar a mano" (lo regenera AdminBolt desde `firewall_rules` en su BD) — si el puerto 2222 se vuelve permanente, hay que darlo de alta desde el panel AdminBolt (Firewall Rules), no solo dejar la regla de runtime. **Pendiente:** que Armando pruebe `ssh -vvv -p 2222 root@82.29.197.33` sin VPN — si conecta, confirma 100% el bloqueo de puerto (router/ISP) y da acceso funcional ya mismo; si falla igual, el problema no es específico del puerto 22. |
| UPD-559 | 02-sep-2026 | Armando | **Fix: subida de video en Archivos de Video (`?m=media_manager`) daba "error de red" y luego "Parte no recibida".** Armando reportó el error al intentar subir un video de >8MB. Causa en cadena, 2 límites de PHP distintos configurados por AdminBolt para el dominio apex.glass (`/etc/opt/remi/php84/php.d/zzz-apex.glass.ini`, `[HOST=apex.glass]`): `post_max_size=8M` y `upload_max_filesize=2M` (más estricto). El chunk de subida en `app/modulos/media_manager.php` estaba fijado en exactamente 8MB — igual al primer límite, así que el overhead de `multipart/form-data` (~700 bytes) lo empujaba por encima y PHP tumbaba la petición completa antes de que la app respondiera nada (se ve como "error de red" en el navegador; confirmado en `php-fpm-error.log`: "POST Content-Length of 8389325 bytes exceeds the limit of 8388608 bytes"). Se bajó el chunk a 6MB — la petición ya pasaba `post_max_size`, pero entonces se topó con el límite más chico (`upload_max_filesize=2M`, aplica por archivo individual dentro de la petición, no por el total) y `api/media_manager.php` respondía "Parte no recibida" (línea 145, `$_FILES['chunk']['error'] !== UPLOAD_ERR_OK`). Chunk bajado de nuevo a **1.5MB**, con margen bajo ambos límites — confirmado por Armando que ya sube el archivo. **Hallazgo adicional en el camino, corregido de una vez:** el SPA (`cargarModulo()` en `app/dashboard.php`) carga cada módulo con `fetch()` sin ningún control de caché — el navegador servía la copia vieja de `media_manager.php` (con el chunk viejo) después de cada edición, obligando a recarga forzada (Ctrl+Shift+R) para ver el cambio; mismo síntoma que UPD-506 pero en la carga de módulos del SPA, no cubierto por ese fix. Agregado `cache: 'no-store'` al `fetch()` de `cargarModulo()` — de aquí en adelante cualquier edición en vivo de un módulo se refleja con un F5 normal, sin necesitar recarga forzada. `php -l` OK en los 2 archivos. |
| UPD-563 | 03-sep-2026 | Mando | **Diagnóstico de fondo del incidente del 2-sep, sesión aparte corriendo como root (`/root/.claude`) — hallazgo real: Cloudflare (activado ~17:53 UTC del 2-sep) ocultaba la IP de todos los visitantes al servidor, no el código ni el VPS. Corregido y verificado. Certificado del sitio investigado, sin resolver, pausado a propósito antes del arranque del turno.** Continuación del plan `/home/mando/.claude/plans/revisame-el-sistema-en-luminous-graham.md` (Pasos 1-2 ya hechos en la sesión anterior como usuario `mando` sin sudo; este UPD cierra Paso 1/2 con verificación root y avanza Paso 3). **P0-1 (el hallazgo más importante):** Apache no tenía `mod_remoteip` configurado, así que desde que el tráfico empezó a pasar por el proxy de Cloudflare, el servidor veía la IP del nodo de Cloudflare en vez de la del usuario real — probado con un login fallido real que quedó registrado en `login_intentos` con una IP de Cloudflare (`104.22.14.216`) en vez de la del cliente. Esto volvía compartidos entre TODA la empresa: los baneos de `fail2ban` (bloquear un nodo de Cloudflare saca a cualquiera que salga por ahí — mismo patrón de "la VPN sí funciona, sin VPN no" reportado por el equipo, porque la VPN cae en otro nodo), el bloqueo de intentos de login (`login_intentos` se llavea por IP+usuario), y volvía inservible cualquier bitácora de IP del sistema. **Fix:** `/etc/httpd/vhosts.d/000-cloudflare-remoteip.conf` (nombre `000-*` para que cargue antes que los vhosts) con `RemoteIPHeader CF-Connecting-IP` + los 24 rangos oficiales de Cloudflare descargados de `cloudflare.com/ips-v4`/`ips-v6` (no de memoria) + localhost. **Lección de método que costó un intento fallido:** el primer archivo se puso en `/etc/httpd/conf.d/`, que `httpd -t` validó como "Syntax OK" — pero ese directorio **no se incluye nunca** en este servidor (confirmado con `httpd -t -D DUMP_INCLUDES`; AdminBolt solo lee `conf/modules-load.conf`, `conf/modules-config.conf` y `vhosts.d/*.conf`), así que el archivo quedó inerte y el `OK` no probaba nada — corregido a `vhosts.d/`. **Verificado end-to-end** dos veces (sesión anterior como `mando`, y de nuevo esta noche como root): la IP registrada por la app pasó de una de Cloudflare a la IP real del visitante; una petición directa al origen (sin pasar por Cloudflare) conserva su IP sin modificar; el log del vhost ya registra la IPv6 real de Telmex de un usuario navegando en vivo. **P0-1 efecto colateral en el informe:** con `conf.d/` confirmado como no-incluido, se determinó que `mod_evasive` (cuya config vivía ahí) nunca ha estado activo en este servidor — baja de "riesgo latente" a "no aplica"; `mod_security` sí está activo (se configura desde `modules-config.conf`, tiene `modsec_audit.log`/`modsec_debug.log` reales). **Paso 2 — fail2ban revisado con las IPs reales ya restauradas:** las 5 jaulas activas, **cero IPs de Cloudflare baneadas** en el momento de la revisión, jaulas de Apache en cero, único baneo activo un bot conocido en SSH ajeno a la oficina — la hipótesis de "un baneo a un nodo de Cloudflare tumbó a medio equipo" era razonable pero no fue lo que ocurrió esa noche en concreto (el síntoma sí es 100% real y viene del mismo hueco P0-1, solo que no vía un baneo activo capturado en el momento de mirar). **Paso 3 — certificado, hallazgo crítico, sin resolver:** el sitio corre con el mismo certificado desde el 14-jun (huella SHA-256 idéntica en disco y en lo que sirve Apache), vence **12-sep-2026 (9 días de margen al momento de este UPD)**. **No hay ningún mecanismo de renovación — nunca existió, no lo rompió Cloudflare:** crontab de root solo tiene las 2 tareas de AdminBolt (`check-for-updates`, `refresh-hosting-accounts-stats`), sin timer de systemd de SSL, sin actividad ACME en el journal de `bolt-agent`. Se investigó `bolt-cli` (tiene comandos propios `run-auto-ssl`/`run-hostname-ssl`/`ssl-health-check`): correr `ssl-health-check` disparó internamente ambos comandos vía el agente local de AdminBolt (puerto 3030) y **los dos fallaron con exit 1** (confirmado en `journalctl -u bolt-agent`, sin detalle del error real porque el agente no captura stdout/stderr al log) — es decir, cuando el panel intenta renovar solo, falla activamente, no solo está ausente. Corriendo el mismo comando directo como root (fuera del agente) dio exit 0 sin ninguna salida, pero **tampoco renovó nada** (mismo archivo, mismo `mtime`, misma huella antes/después) — un no-op silencioso, causa todavía sin explicar. Se probó (autorizado) que la ruta de validación ACME (`/.well-known/acme-challenge/`) está despejada — Cloudflare la deja pasar al origen sin bloquear. **Investigación pausada aquí a propósito, sin usar `--force`:** el usuario interrumpió antes de escalar a `run-auto-ssl --force` por priorizar que el sistema estuviera 100% estable para el arranque del turno en pocas horas, en vez de seguir escarbando el certificado esa misma madrugada — decisión correcta, quedan 9 días de margen real. **Verificación de salud en vivo, todas las IPs, no solo la de quien opera:** con Paso 1/2 aplicados, se revisó el tráfico real del día completo (no solo pruebas propias) — **0 códigos 401/5xx en todo el log**, **0 usuarios bloqueados** en `login_intentos` (solo 2 filas de prueba, limpiadas), **0 IPs de Cloudflare baneadas**, DNS resuelve igual desde Cloudflare (1.1.1.1) y Google (8.8.8.8) sin ningún split-DNS que trate distinto a la oficina. El único 403 que se repetía cada ~60s en el log (`api/autorizaciones.php`) es ruido normal y preexistente — ese endpoint rechaza a cualquier rol que no sea `dir_admin`/`desarrollo` a propósito, y el frontend de `dashboard.php` (línea ~895) lo traga en silencio (`.catch(function(){})`) sin cerrar sesión ni mostrar error; no es nuevo ni parte del incidente. **Conclusión de la noche: el sistema debería estar arriba al 100% para todos los roles/IPs cuando entre el equipo** — la causa de fondo de "me saca"/"no me deja entrar" (P0-1) ya está corregida y verificada con tráfico real, independiente del certificado (que sigue vigente 9 días más). **Pendiente (ver también sección 12):** cerrar la investigación del certificado sin prisa (P1-2), reponer y blindar el workaround de SSH puerto 2222 que UPD-562 dejó en runtime y ya no está activo tras el reinicio del VPS (P1-1, sin tocar esta sesión — no confirmar con Armando que "el 2222 no funciona" sin decirle primero que la prueba de UPD-562 nunca fue válida, el puerto nunca llegó a abrirse en firme), journal persistente (P1-3, `/var/log/journal` no existe — toda la evidencia previa al reinicio del 2-sep se perdió y volvería a perderse), blindar contra AdminBolt los 3 archivos que regenera en silencio (P1-4: `zzz-apex.glass.ini`, `sshd_config.d/01-custom.conf`, y ahora también `vhosts.d/000-cloudflare-remoteip.conf` — este último con precedente a favor, `panel-apex-glass-http.conf` es manual y sobrevive en esa carpeta desde el 27-jun), MX/SPF de `apex.glass` apuntando todavía al HostGator cancelado (P2-1, requiere Fase B con Armando en el panel de Cloudflare), y no restringir el origen 443 a los rangos de Cloudflare (P0-2) hasta que Armando tenga el panel de Cloudflare a la mano (riesgo real de dejar a toda la empresa fuera sin forma de revertir desde la terminal). Limpiadas las filas de prueba `portal:ZZTEST-%` en `login_intentos`. |

| UPD-565 | 03-sep-2026 | Armando | Corrección de datos (no código): **S-723** (orden id=986, COT-1544) se había registrado por error como "Recolección en planta" en vez de "Entrega a domicilio" (la cotización sí decía domicilio; el error fue al elegir el tipo de salida en `imprimir_salida.php` al momento de imprimir/registrar). Eso ya había cerrado la orden (`estado='entregada'`, `fecha_cierre` del mismo día) y marcado su única pieza (id=6606) como `entregado`, sin pasar por Rutas (tipo recolección no activa `requiere_ruta`). Revertido dentro de una transacción con SELECT de verificación antes/después: pieza 6606 `entregado`→`terminado`; borrado el registro de salida errónea (`orden_salidas` id=609 + su detalle en `orden_salida_piezas`); orden 986 `entregada`→`activa`, `fecha_cierre` limpiado, `requiere_ruta=1` para que aparezca en Logística Rutas → Pendientes de asignar. `tipo_entrega` de la cotización y de la orden ya estaban en `domicilio`, sin necesidad de tocarlos. El WhatsApp de "pasa a recoger a planta" (plantilla `salida_recoleccion`) ya se había enviado al cliente al momento del error — a petición de Armando NO se mandó ningún mensaje de corrección, solo se avisará al cliente cuando se programe la ruta real. |
| UPD-566 | 05-sep-2026 | Armando | **Nuevo: recibo imprimible del Bono de Corte — Pedacería.** Armando pidió poder generar el recibo del pago del bono (módulo `?m=bono_corte`, UPD reciente de Mando) para dárselo firmado a Angel. Antes solo existía el botón "Marcar como pagado" en BD, sin ningún comprobante imprimible. Nuevo `app/imprimir_bono_corte.php` (mismo estilo visual que `imprimir_apartado.php`: header APEX GLASS, Syncopate, doc-title-bar navy) — muestra operador, semana laborada, m² de pedacería aprovechados, monto, fecha/hora de pago, quién autorizó, nota breve de la fórmula ($150 por cada tramo completo de 18 m², primer tramo proporcional) y área de firmas ("Recibí conforme" del operador + "Autorizó el pago"); si el registro todavía está en estado `calculado` (no pagado), el archivo se niega a imprimir con un mensaje explícito en vez de mostrar un recibo de algo que no se ha pagado. `api/bono_pedaceria.php` `accion=resumen_semana` — se agregó `id` al SELECT de `bono_pedaceria_pagos` y se expone como `pago_id` en la respuesta (antes no se exponía el id del pago individual, solo estado/aprobado_por/aprobado_at, así que no había forma de enlazar un recibo a un pago específico). `app/modulos/bono_corte.php` — botón nuevo "Imprimir recibo" junto al pill "Pagado ✓" de cada operador (visible solo cuando `estado==='pagado'` y existe `pago_id`), abre `imprimir_bono_corte.php?id=<pago_id>` en pestaña nueva. **2 rondas de fix de ruta, ambas confirmadas por Armando el mismo día:** (1) el archivo vive en `app/` (no en la raíz de `produccion/`), igual que el resto de los `imprimir_*.php` — Armando probó primero pegando la URL a mano sin `/app/` y dio "File not found"; correregida la URL manual funcionó. (2) el botón "Imprimir recibo" del propio módulo seguía roto porque el `onclick` usaba `../imprimir_bono_corte.php` — el SPA nunca cambia la URL del navegador (se queda en `app/dashboard.php`), así que una ruta relativa dentro del HTML inyectado por un módulo se resuelve contra `app/`, no contra `app/modulos/` de donde viene el archivo fuente; el `../` de más mandaba a `produccion/imprimir_bono_corte.php` (inexistente). Corregido a ruta relativa simple `imprimir_bono_corte.php` (mismo patrón ya usado en `finanzas_cobranza.php:595` para `imprimir_salida.php`) — confirmado funcionando por Armando tras el fix. `php -l` limpio en los 3 archivos + `node --check` del `<script>` embebido en el módulo. Verificado contra datos reales de BD (Angel Baltazar, 5 semanas pagadas) antes de la prueba visual. |
| UPD-564 | 03-sep-2026 | Armando | **Fix: columna "Entrega" en `app/imprimir_salida.php` (remisión) mostraba conteo acumulado en vez del rango de la entrega específica.** Armando reportó que en una orden con 2 entregas parciales (4+4 de 22 láminas), la 2a impresión decía "8 de 22" (acumulado de ambos envíos) en vez de indicar cuáles piezas iba ESTA entrega en concreto. Fix: la columna ahora muestra el rango por número de pieza del envío más reciente que tocó esa partida — "5 al 8 de 22 al 03/sep/2026" — tanto en el render PHP inicial (`formatRangoPiezas()`, usa `$pieza_salida_idx` para saber qué salida tocó cada pieza por última vez) como en la actualización JS en vivo justo al confirmar una nueva entrega (`actualizarCeldasEntrega()`). Piezas no consecutivas se agrupan en tramos ("5 al 8, 11"). **Seguimiento en la misma sesión** (pregunta de Armando "¿y si quiero reimprimir la primera entrega?"): agregada capacidad de reimprimir cualquier entrega histórica específica, no solo el estado actual — nuevo mapa `PIEZAS_MAP` (id→partida/pieza_num de TODAS las piezas de la orden, embebido en JS) + función genérica `estadoEntregaCeldas(cutoffIdx)` que recalcula la columna Entrega "como si el tiempo estuviera congelado" justo después de la entrega `cutoffIdx` (ignora entregas posteriores); usada tanto por el botón "Reimprimir" genérico (cutoff = última entrega, estado actual) como por el link nuevo "Reimprimir esta" en cada renglón del historial "Entregas registradas" del menú (`reimprimirEntrega(idx)`, cutoff = esa entrega específica) — esta última agrega un badge azul "REIMPRESIÓN ENTREGA N DE M" junto al título para no confundirla con el estado actual, y restaura la fecha de entrega original del header al volver al "Reimprimir" genérico. Verificado con `php -l`, `node --check` del `<script>` embebido, y simulación de la lógica completa contra los datos reales de la orden 990 (S-990, cotización COT-1557): entrega 1 (piezas 6617-6620) da "1 al 4 de 22", entrega 2 (piezas 6621-6624) da "5 al 8 de 22" — cada reimpresión aislada en su propio rango, sin mezclarse. Sin prueba visual en navegador (no hay Chrome DevTools/Playwright MCP en esta sesión) — pendiente que Armando lo confirme viéndolo en vivo. |

| UPD-567 | 07-sep-2026 | Armando | **Checador ZKTeco MB20-VL — Fase 1 (hardware/red) completada y probada de punta a punta; Fase 2 (software) pausada para construir primero el módulo de RH (ver UPD-568).** Objetivo del proyecto: conectar el reloj checador físico de planta al sistema para automatizar asistencia hacia Nómina. Hardware: Raspberry Pi 4 (4GB) + extender Tenda AC1200 en modo repetidor, comprados por Armando. Reloj (`MB20-VL`, firmware `ZMM510_TFT`, número de serie `SPK7251000343`) conectado por Ethernet al extender con IP fija `192.168.15.30` (fuera del rango DHCP del router, que empieza en `.100` — sin riesgo de choque). Pi flasheada con Raspberry Pi OS Lite 64-bit vía Raspberry Pi Imager, IP `192.168.15.171` (DHCP), acceso solo por SSH (se evaluó y **se descartó a propósito instalar Tailscale en el VPS de producción** para administrarla directo — el túnel privado Pi↔VPS hubiera sido un brinco potencial hacia el VPS si la Pi en planta se comprometía algún día, saltándose Cloudflare/fail2ban/firewall; Tailscale sí quedó instalado únicamen la Pi, tailnet `tnglass.com.mx`, sin conectar al VPS). **Arquitectura final: push vía protocolo ADMS nativo del reloj** (no pull con `pyzk`, que falló de forma inconsistente — "broken pipe" y luego "timed out" incluso tras quitar HTTPS — se abandonó esa vía por completo, no se necesita). El reloj ya traía **ADMS activado de fábrica** apuntando a una IP de otra red sin usar; se repuntó a la Pi (`192.168.15.171:8081`) y se construyó `~/adms_listener.py` en la Pi (Python `http.server`, sin dependencias extra) que implementa el handshake básico (`GET/POST /iclock/cdata`, `GET /iclock/getrequest`) y por ahora solo imprime lo recibido — es de prueba, falta la versión de producción. **Hallazgo clave del troubleshooting:** el reloj tenía "HTTPS" activado en Comm/PC Connection, lo cual bloqueaba las conexiones ADMS (el listener de prueba solo habla HTTP plano) — se desactivó y el push empezó a funcionar de inmediato. **Verificado con datos reales:** llegó el backlog completo de `OPERLOG` (bitácora de operaciones del reloj desde dic-2025, incluyendo timestamp exacto de los cambios de ADMS hechos ese mismo día — confirma sincronización al corriente) y una checada real de prueba en `ATTLOG` (formato confirmado: `PIN | FechaHora | Estatus | VerifyMode | ...reservados`) — se detectó y corrigió a tiempo un desfase de zona horaria en el reloj (ajustada la hora antes de confiar en los timestamps). El reloj **ya traía 29 empleados reales enrolados desde antes** (con meses de historial, no es un aparato nuevo) — lista PIN↔nombre capturada de un reporte semanal que Armando ya sacaba manualmente del reloj, incluye personal reconocible del sistema (ID 104=Lina Rocha, ID 11=Víctor Bautista, ID 202=Angel Baltazar). Alcance de administración confirmado con Armando: desde Apex se podrá dar de alta/editar/baja de empleados, ver historial de checadas, e incluso borrar remotamente un usuario del reloj (el protocolo ADMS soporta comandos servidor→dispositivo) — **lo único que exige presencia física es el enrolamiento inicial de huella/rostro**, limitación inherente del hardware biométrico. **Pendiente para Fase 2** (ver sección 12): construir el listener de producción (parseo real + reenvío a `api/checador.php` nuevo en el VPS + buffer local si se cae internet), diseñar el mapeo PIN↔`nomina_empleados` (tabla nueva, ya no hay solo 1 empleado de prueba sino 29 reales por dar de alta — ver UPD-568 para el expediente), y resolver si el checador calcula sueldo automático o solo queda de respaldo junto a la captura manual. |
| UPD-568 | 07-sep-2026 | Armando | **Nuevo módulo: Recursos Humanos — expediente, documentos, vacaciones e incidencias por empleado.** Pausa del proyecto de checador (UPD-567) a petición de Armando para construir primero esto, ya que la antigüedad capturada aquí (`fecha_ingreso`) es la que después alimentaría el cálculo de vacaciones y eventualmente el checador/Nómina. **Alcance confirmado:** acceso solo `dir_admin`/`administracion`/`dueno`/`desarrollo` — Jefe de Piso explícitamente excluido ("no ve esta información ni trabaja con ella"); documentos aceptan imagen (jpg/png) y PDF. **Checkpoint antes de tocar producción:** tag git `pre-modulo-rh-20260907` + backup completo de BD `_backups/pre_modulo_rh_20260907.sql` (14.6MB) — cambios de BD 100% aditivos (columnas NULL nuevas + tablas nuevas), reversible sin tocar datos existentes. **BD:** `nomina_empleados` +11 columnas (`fecha_ingreso` — crítico, de ahí sale la antigüedad —, `fecha_nacimiento`, `foto`, `curp`, `rfc`, `nss`, `telefono`, `direccion`, `contacto_emergencia_nombre`, `contacto_emergencia_telefono`, `fecha_baja`); 4 tablas nuevas: `rh_documentos` (documentos genéricos del expediente), `rh_vacaciones_periodos` (un renglón por año de antigüedad cumplido, con `dias_derecho` calculado automático según la tabla de vacaciones dignas LFT 2023: año1=12, año2=14, año3=16, año4=18, año5=20, años6-10=22, +2 cada 5 años después — verificado con `php -r` contra la tabla oficial hasta el año 22), `rh_vacaciones_detalle` (cada registro individual, tipo `tomado` o `pagado_sin_ausencia` — a petición de Armando de poder pagar la semana sin que el empleado se ausente, sin perder el control del saldo), `rh_incidencias` (incapacidad enfermedad/maternidad/paternidad, riesgo de trabajo, fallecimiento familiar, permisos con/sin goce, con documento de soporte opcional ligado a `rh_documentos` — un documento en uso por una incidencia no se puede borrar). **2 permisos nuevos** `ver_rh`/`gestionar_rh` en `api/permisos.php`, agregados a los mismos 4 roles que ya tienen `ver_contabilidad`. **`api/nomina.php`** extendido (no duplicado) — `crear_empleado`/`editar_empleado` ya aceptan los campos nuevos del expediente, mismo patrón campo-por-campo que ya usaba. **`api/rh.php` nuevo** — CRUD de documentos/vacaciones/incidencias + cálculo automático de periodos de vacaciones al abrir el expediente (`rhAsegurarPeriodos()`, genera los años faltantes sin pisar los ya capturados) + subida de archivos replicando exactamente el patrón de seguridad de `api/archivos_ordenes.php` (extensión + MIME real vía `finfo` + límite 10MB + carpeta `archivos_rh/` con `.htaccess Deny from all`, servida solo vía script autenticado con `Content-Disposition: inline`) — se agregó de más limpieza de la foto anterior al reemplazarla (evita archivos huérfanos) y validación de formato de fecha antes de calcular días en incidencias (evita error fatal con dato mal formado). **`app/modulos/rh_empleados.php` nuevo** — tabla de empleados con foto/antigüedad + modal de detalle con 4 pestañas (Expediente/Documentos/Vacaciones/Incidencias), siguiendo el patrón SPA obligatorio (namespace `var ModRH`, sin template literals, sin arrow functions en onclick). **Sidebar** (`app/dashboard.php`): nueva entrada "Recursos Humanos" dentro de la sección Finanzas, gateada por `ver_rh`; agregado al mapa `MODULOS` del router SPA. **Probado con dry-run real en BD** (`BEGIN`/`ROLLBACK`, empleado de prueba con 3 años de antigüedad simulados): los 3 periodos se generaron con los días correctos (12/14/16), el saldo de vacaciones se descontó bien al registrar 5 días tomados (12→7), el candado de "no borrar documento en uso por una incidencia" se comportó como se esperaba — 0 filas de prueba quedaron en producción, verificado con SELECT posterior. `php -l` con el binario real de producción (`/usr/bin/php84` — el `php` por default de la sesión root es 8.0 y no sirve para probar nada que cargue `vendor/autoload.php`, lección para futuras sesiones) limpio en los 5 archivos tocados/creados + `node --check` del `<script>` embebido. **Prueba visual completada en UPD-569** (Playwright MCP, misma sesión root del 07-sep-2026) — empleado, foto, documento, vacación e incidencia probados en navegador; se encontró y corrigió ahí mismo un gap real (incidencias sin forma de adjuntar documento de soporte pese a que la BD/API ya lo soportaban). |

| UPD-569 | 07-sep-2026 | Armando | **Prueba visual completa del módulo de RH (UPD-568) vía Playwright MCP, más un fix real encontrado en el camino: incidencias no tenía forma de adjuntar el documento de soporte (ej. el informe de incapacidad del IMSS) aunque la BD y el API ya lo soportaban.** Prueba de punta a punta con datos reales de navegador (no dry-run): usuario temporal `desarrollo_claude` (rol `desarrollo`, borrado al terminar) para no tocar la cuenta `desarrollo` existente ni pedir credenciales de Armando/Mando. Flujo probado: alta de empleado con fecha de ingreso hace 3 años → los 3 periodos de vacaciones se generaron solos con los días correctos (12/14/16, tabla LFT 2023); subida de foto de perfil (jpg) — se refleja en la miniatura de la tabla; subida de documento genérico (pdf) en la pestaña Documentos — aparece con "Ver"/"Borrar"; registro de 5 días de vacaciones tomados en el año 3 — el saldo bajó de 16 a 11 correctamente; registro de una incidencia (incapacidad, 3 días) — aparece en la lista. **Hallazgo real (no solo confirmación):** al querer probar "adjuntar el informe del IMSS a la incapacidad" como pidió Armando, el formulario "Registrar incidencia" en `app/modulos/rh_empleados.php` no tenía ningún campo de archivo — aunque `rh_incidencias.documento_id` y `api/rh.php` (`accion=incidencia_crear`) ya aceptaban un documento ligado desde que se construyó UPD-568 (incluido el candado de "no borrar un documento en uso por una incidencia"). Se agregó el campo "Documento de soporte (opcional)" al formulario; `crearIncidencia()` ahora, si hay archivo seleccionado, primero lo sube vía `accion=subir_documento` (tipo fijo "Soporte de incidencia") y liga el `id` resultante como `documento_id` al crear la incidencia; `renderIncidencias()` agrega un link "Ver documento" cuando la incidencia trae uno. Sin cambios de backend — solo el gap de UI. Probado end-to-end en el navegador: incidencia creada con PDF adjunto quedó ligada (`documento_id` correcto, confirmado en BD) y el link "Ver documento" abre el archivo; la incidencia sin adjunto (creada antes) correctamente no muestra el link. `php -l` + `node --check` del `<script>` embebido OK. **Nota de infraestructura:** el registro del MCP de Playwright (`claude mcp add ... --browser chromium`) tenía un valor de `--browser` inválido para esta versión del paquete (los válidos son chrome/firefox/webkit/msedge, no "chromium") — arrancaba igual pero intentaba usar el canal "chrome-for-testing" sin descargar, fallando con "Browser not installed". Resuelto con `npx @playwright/mcp install-browser chrome-for-testing` (177MB, no requirió tocar el registro ni reiniciar la sesión) — queda pendiente en algún momento corregir el registro (`claude mcp remove` + `add` con `--browser chrome --executable-path /usr/bin/chromium-browser`) para no depender del canal descargado si se vuelve a perder el cache de Playwright. **Limpieza:** todo el rastro de la prueba se borró al terminar — empleado de prueba + sus documentos/vacaciones/incidencias en BD, los 3 archivos físicos subidos (`archivos_rh/`), y el usuario `desarrollo_claude` — verificado con SELECT posterior, 0 filas de prueba quedaron en producción. Agregado `.playwright-mcp/` a `.gitignore` (capturas/snapshots de la sesión de prueba, no debían trackearse). Con esto queda cerrado el pendiente de UPD-568 "falta prueba visual" — el módulo de RH pasa a **HECHO y probado**, no solo construido. |

| UPD-570 | 08-sep-2026 | Armando | **Checador ZKTeco — lado VPS de "alta/baja vía Apex" construido (mitad de la feature; falta el lado Pi).** Continuación de la feature abierta el 07-sep-2026 dentro de UPD-568/569: crear/borrar el usuario del reloj (PIN+nombre) desde la UI de RH, con la arquitectura ya decidida de que todo el tráfico Pi↔VPS lo inicia siempre la Pi (nunca al revés). Checkpoint: tag git `pre-checador-alta-baja-20260908` + backup `_backups/pre_checador_alta_baja_nomina_empleados_20260908.sql`. **BD (aditivo):** `nomina_empleados` +columna `checador_pin` (INT UNSIGNED, UNIQUE, NULL); tabla nueva `checador_comandos` (cola: `empleado_id`, `tipo` alta/baja, `pin`, `nombre_reloj`, `estado` pendiente→entregado→confirmado/error, `creado_por`, timestamps). **`api/checador.php` nuevo — dos mundos en un archivo:** (1) lado UI (sesión, `ver_rh`/`gestionar_rh`): `encolar_alta` (reserva el PIN en `nomina_empleados` + inserta comando — rechaza PIN duplicado o empleado que ya tiene uno), `encolar_baja` (toma el PIN ya asignado, rechaza si ya hay una baja en curso), `historial` (por empleado), `reintentar` (por si un comando se queda en "entregado" sin confirmar por corte de internet en la Pi — reabre a "pendiente"); (2) lado dispositivo (llave compartida en header `X-Checador-Key`, sin sesión): `pendientes` (GET — la Pi hace polling saliente, se marca "entregado" atómico con `FOR UPDATE`) y `confirmar` (POST `{id,ok,mensaje}` — si confirma baja o rechaza alta, libera el PIN). Llave nueva `CHECADOR_LISTENER_KEY` generada random (32 bytes) en `.env` + `config.php`. **UI (`app/modulos/rh_empleados.php`):** 5ª pestaña "Checador" en el expediente — estado actual (sin registrar / alta en proceso / activo con PIN / baja en proceso) con su botón correspondiente, historial de comandos con opción "Reintentar", aviso explícito de que el enrolamiento de huella sigue requiriendo presencia física. **Probado:** dry-run completo en BD (`BEGIN`/`ROLLBACK`) del ciclo alta→confirmado→baja→confirmado, verificando que el PIN se reserva y se libera en los momentos correctos — 0 filas de prueba quedaron en producción; prueba real por HTTPS del endpoint de dispositivo (con/sin/mala llave → 200/401/401). `php -l` + `node --check` del `<script>` embebido, OK. **Incidente real durante la prueba (causa y fix, ya resuelto):** al editar `.env` con la herramienta de escritura (sesión root), el archivo quedó `root:root` — le quitó el permiso de lectura a `apexglass2025` (dueño real, permisos `640`), que es quien corre PHP-FPM; durante esa ventana **todo el `.env` se leyó vacío para cualquier request real, incluyendo `DB_PASS`**, y el sitio completo cayó con "No database selected" (~4 minutos, 01:40–01:44 UTC, confirmado en `php-fpm-error.log` con tráfico real de producción, no solo pruebas propias). Corregido con `chown apexglass2025:apexglass2025 .env && chmod 640 .env`; verificado que no quedó ningún error posterior y que el sitio responde normal. Lección documentada en sección 1 para que no se repita. **Pendiente para cerrar la feature completa** (ver sección 12): construir el listener de producción en la Raspberry Pi que consuma `GET ?accion=pendientes` y reporte con `POST ?accion=confirmar` (contrato ya definido arriba) — requiere sesión con acceso a la Pi; cargar los 29 empleados reales ya enrolados en `nomina_empleados` (lista PIN↔nombre pendiente de que Armando la vuelva a compartir). |

| UPD-571 | 08-sep-2026 | Armando | **Primeros 3 empleados reales cargados en `nomina_empleados` con su PIN del reloj checador, para probar la pestaña Checador de RH (UPD-570) con datos reales.** Armando volvió a pasar el reporte "Attendance Record Report" que ya sacaba manualmente del reloj — de ahí se recuperó la lista PIN↔nombre completa de los 29 empleados enrolados (perdida desde UPD-567) más el ID 1 "Admin" (cuenta administrativa del dispositivo, no una persona, excluida). Antes de cargar los 29, Armando aclaró que **más de la mitad ya no trabajan en la empresa** — decidió no cargarlos a todos para no tener que ir borrando después; pidió cargar solo 3 para probar el flujo: **id=6 Angel Baltazar (PIN 202, planta), id=7 Jose Martinez Ledezma (PIN 203, planta), id=8 Lina Rocha (PIN 104, oficina)** — uno de los tres lo va a dar de baja a propósito para probar que ese lado del flujo funciona. Carga directa de `checador_pin` (sin pasar por la cola `checador_comandos`) porque ya están físicamente enrolados en el reloj — no hace falta un comando de "alta". Sin `puesto`/`fecha_ingreso`/`sueldo_base` todavía (Armando dijo que esos datos del expediente "los vemos después"). **El resto de la lista de 29 (con 4 nombres incompletos del reloj — solo trae "Junol", "Jose Luis", "Gilberto", "Kevin Alexis" sin apellido completo) queda documentada aquí por si se retoma la carga completa:** 203 Jose Martinez Ledezma, 104 Lina Rocha, 205 Juan Montelongo, 206 Juan Garcia Sanjuan, 208 Jose Gonzalez Lopez, 209 Jordi Reyes Ovalle, 210 Jorge Segundo Rojas, 213 Bernardo Leal Lara, 102 Beatriz Rocha Briones, 103 Cynthia Negrete Carrillo, 2 Humberto Francisco, 3 Junol, 5 Javier Reyes, 6 Jose Luis, 4 Antonio Monsivais, 8 Irene Medellin, 10 Ramses Juarez, 11 Victor Bautista, 12 Francisco Vazquez, 13 Mario Martinez, 14 Kevin Saucedo, 15 Oscar Cantu, 18 Gilberto, 19 Daniel Monsivais, 20 Manuel Cruz, 22 Isaac Morales, 23 Kevin Alexis, 24 Yahaira Flores (más 202/104 ya cargados). Verificado con SELECT antes (0 choques de PIN, tabla vacía) y después del INSERT. Sin cambios de código — solo datos. **Pendiente:** que Armando pruebe la baja de uno de los tres (falta aclarar si se refiere a dar de baja del reloj vía la pestaña Checador, marcar inactivo en el expediente, o borrar el registro por completo — cada una es una acción distinta) y decidir si se carga el resto de la lista o solo se van agregando los que de verdad siguen activos. |

| UPD-572 | 08-sep-2026 | Armando | **Nómina pasa de captura mensual a semanal + 2 bonos nuevos (Puntualidad/Asistencia y Productividad) que varían por persona según su asistencia.** Armando aclaró que el sueldo que captura en `sueldo_base` es el neto **por semana** (no mensual, que era el supuesto original del módulo WIP de UPD-417), y que el personal de planta recibe además esos dos bonos variables que también hay que registrar. Antes de tocar nada se le preguntó explícitamente (evitando adivinar sobre dinero): confirmó pasar el registro de pagos de mensual a semanal, y que ambos bonos se capturan a mano por ahora (todavía no hay datos reales de asistencia del checador — falta el listener de la Pi, UPD-570). Checkpoint: tag git `pre-nomina-semanal-bonos-20260908` + backup `_backups/pre_nomina_semanal_bonos_20260908.sql` (tabla `nomina_pagos` estaba vacía, 0 filas reales — sin datos que migrar). **BD:** `nomina_pagos` — se quita `periodo CHAR(7)` (y su único índice), se agregan `semana_inicio`/`semana_fin` (DATE, mismo criterio Lunes-Domingo que ya usa `bono_pedaceria_pagos`/`api/bono_pedaceria.php`) + `bono_puntualidad_asistencia`/`bono_productividad` (DECIMAL 10,2), único ahora por `(empleado_id, semana_inicio)`. **`api/nomina.php`:** función `nomSemana()` (copia del mismo cálculo Lunes-Domingo de `bpSemana()` en `bono_pedaceria.php`, para que "semana" signifique lo mismo en todo el sistema); `accion=pagos` ahora recibe `semana` (cualquier fecha dentro de la semana deseada, ya no `periodo=AAAA-MM`); `accion=guardar_pago` acepta `semana_inicio` (fecha de referencia, se normaliza server-side) + los 2 campos de bono, y el `total_pagado`/póliza automática (`pl_generarAutomatica`) ahora suma sueldo neto + ambos bonos + IMSS patronal + otras prestaciones — sigue cayendo en la misma cuenta contable 5.2 (planta) / 6.1 (oficina) que ya existía, sin crear cuentas nuevas. **`app/modulos/nomina.php`:** selector de "Periodo" (input mes) reemplazado por navegación de semana con `‹ › ` + etiqueta "7 sep – 13 sep" (mismo patrón visual que ya usa `bono_corte.php`); tabla con 2 columnas nuevas editables (Bono Puntualidad/Asist., Bono Productividad); banner WIP actualizado para reflejar captura semanal. Probado con dry-run real en BD (`BEGIN`/`ROLLBACK`) usando a Angel Baltazar (id=6, ya cargado en UPD-571): INSERT de una semana completa con ambos bonos, verificado que el JOIN por `semana_inicio` encuentra el mismo registro sin importar qué día de esa semana se consulte (miércoles cae en la semana del lunes correcto) — 0 filas quedaron en producción. `php -l` + `node --check` del `<script>` embebido en ambos archivos, OK. **Pendiente:** prueba visual en navegador (sin Chrome DevTools/Playwright en esta sesión) — falta que Armando capture un pago real de la semana en curso y confirme que la navegación `‹ ›` y los montos se ven y guardan bien. Los bonos siguen siendo captura manual — el día que el checador esté en vivo (falta el listener de la Pi) se podría explorar pre-llenarlos según asistencia real, pero eso no se construyó hoy. |

| UPD-573 | 08-sep-2026 | Armando | **"Dar de baja" combinado en RH — inactivar en Apex + quitar del reloj checador, en una sola acción.** Armando aclaró que quería probar exactamente esto (no las 3 opciones por separado que se le habían planteado): cuando alguien renuncia/se da de baja, debe quedar **inactivo laboralmente en Apex con su expediente completo intacto** (documentos, vacaciones, incidencias — nada se borra) y **al mismo tiempo** encolarse la baja de su registro en el reloj físico. Antes de esto, `nomina_empleados.fecha_baja` era un campo suelto en el expediente que no cambiaba `activo` ni tocaba el checador — se quedaba como dato inerte. **`app/modulos/rh_empleados.php`:** nuevo panel "Estado laboral" al inicio de la pestaña Expediente — muestra "Activo" + botón "Dar de baja", o "Baja — [fecha]" + botón "Reactivar"; el botón de baja abre un modal pidiendo la fecha y, al confirmar, encadena dos llamadas: `PUT api/nomina.php?accion=editar_empleado` (`activo=0`, `fecha_baja`) y, si el empleado tiene `checador_pin` asignado, `POST api/checador.php?accion=encolar_baja` (best-effort — si ya hay una baja en proceso o falla la conexión, avisa pero no revierte la baja en RH, que es la parte que sí debe quedar firme). "Reactivar" solo regresa `activo=1`/`fecha_baja=NULL` — no vuelve a dar de alta en el reloj automáticamente (eso es una acción aparte si aplica). Sin cambios de backend — `api/nomina.php` (`editar_empleado`) y `api/checador.php` (`encolar_baja`, UPD-570) ya soportaban esto por separado, solo faltaba la UI que los encadenara. Nota de permisos: `editar_empleado` exige `gestionar_contabilidad`, no `gestionar_rh` — confirmado que viajan juntos en los 4 roles que tienen `gestionar_rh` (dir_admin/administracion/dueno/desarrollo), mismo acoplamiento que ya tenía "Guardar expediente" desde UPD-568, no es nuevo. Probado con dry-run real en BD (`BEGIN`/`ROLLBACK`) sobre Jose Martinez Ledezma (id=7, PIN 203, cargado en UPD-571): tras la baja simulada queda `activo=0`, `fecha_baja` con el dato, comando `baja` encolado con su PIN, y el registro sigue existiendo completo — 0 cambios quedaron en producción tras el rollback. `php -l` + `node --check` del `<script>` embebido, OK. **Candado añadido a petición de Armando (misma sesión, antes de la primera prueba real):** el modal de "Dar de baja" ahora pide escribir el PIN del reloj del empleado como segundo paso de confirmación cuando tiene uno asignado — si no coincide con `empleadoActual.checador_pin`, se rechaza con alerta y no se manda nada al backend; si el empleado nunca tuvo PIN, el campo se oculta y no aplica. Es un candado de UX (evitar dar de baja al empleado equivocado por un clic en la fila incorrecta), no de seguridad — la autorización real la sigue dando la sesión con `gestionar_rh`. **Confirmado en producción por Armando (primera prueba real, no dry-run):** dio de baja a Jose Martinez Ledezma (id=7) — quedó `activo=0`, `fecha_baja=2026-09-08`, y el comando de baja se encoló correctamente con su PIN 203 en `checador_comandos` (verificado en BD). **Bug real encontrado en la misma prueba, corregido de inmediato:** al quedar inactivo, el empleado desapareció por completo de la tabla de RH sin ninguna forma de volver a encontrarlo — `api/rh.php` (`accion=listar`) ya soportaba el parámetro `activos` para traer inactivos, pero `app/modulos/rh_empleados.php` nunca lo usaba ni tenía ningún filtro en pantalla. Se agregó un selector "Activos / Bajas / Todos" junto al buscador; el frontend ahora siempre trae la lista completa (`activos=0`) y filtra en pantalla combinando ese selector con el texto de búsqueda — así se puede reactivar a alguien sin perder de vista su expediente. `php -l` + `node --check` OK. Con esto la feature queda **verificada en producción de punta a punta** (RH inactivo + candado de PIN + comando de baja encolado + poder volver a encontrar al empleado) — solo sigue pendiente que la baja realmente llegue al reloj físico, que depende del listener de la Pi (aún no construido). |

**Nota de la misma sesión — nómina es outsourcing, no aplica hoy:** Armando aclaró que él paga a un despacho externo para la dispersión de nómina y no se involucra a fondo en el módulo `?m=nomina` (UPD-572) por eso — su prioridad real hoy es RH + checador. Ver regla nueva en sección 1 sobre su semana real de nómina (jueves-miércoles, no lunes-domingo) para cuando se construya el reporte semanal hacia el despacho (pendiente, sección 12).

| UPD-574 | 08-sep-2026 | Mando | **Checador ZKTeco — listener de producción en la Raspberry Pi construido, desplegado y confirmado contra el reloj físico real (cierra la Fase 2 abierta en UPD-570/571/573).** Escrito `checador_listener.py` (Python 3 estándar, sin dependencias) corriendo en la Pi: sirve el protocolo ADMS del reloj MB20-VL en el puerto 8081 (handshake `/iclock/cdata`, entrega de comandos por `/iclock/getrequest`, confirmación por `/iclock/devicecmd`) y hace polling saliente cada 15s a `api/checador.php?accion=pendientes`, reportando resultado a `accion=confirmar` — arquitectura Pi-inicia-siempre ya documentada en UPD-570. **Hallazgo real en el despliegue: Cloudflare bloqueaba con 403 cualquier request con el User-Agent por default de Python** (`Python-urllib/3.x`) antes de que llegara siquiera a Apache (confirmado con el request de la Pi ausente por completo del `access_log`, y reproducido a voluntad desde el propio VPS con `curl -A "Python-urllib/3.11"` → 403) — primera evidencia concreta del pendiente ya anotado en UPD-563 sobre Bot Fight Mode/WAF de Cloudflare bloqueando tráfico legítimo no-browser. Fix: User-Agent propio (`ApexGlass-Checador-Listener/1.0`) en las peticiones del listener — confirmado que resuelve el bloqueo. **Incidente de seguridad menor durante el despliegue, ya cerrado:** para bajar el script a la Pi sin que el copy/paste por SSH lo corrompiera (varias apps SSH reindentan heredocs — mismo síntoma que el ya documentado en sección 1 con AdminBolt/terminales), se probaron heredoc (falló, `SyntaxError` por caracteres corruptos) y finalmente un archivo temporal servido sin login desde `produccion/dl_<token-random>/` por unos minutos, confirmado por hash y borrado de inmediato tras la descarga. Por disciplina se trató la `CHECADOR_LISTENER_KEY` como comprometida aunque la URL era un token aleatorio de 32 caracteres hex improbable de adivinar: **rotada** (nueva llave en `.env`, `config.php`, verificado que la vieja ya rechaza con 401 y la nueva acepta), y reflejada en el script ya entregado. **Prueba real de punta a punta (no simulada):** se reactivó con el mecanismo de "reintentar" un comando de baja que sin querer se había consumido de más durante las pruebas de la llave/User-Agent (quedó en estado "entregado" sin que el listener real llegara a verlo) — el listener lo recogió, se lo entregó al reloj (`C:5:DATA DELETE USERINFO PIN=203`), el reloj respondió `Return=0` (éxito) y sus propios contadores de usuarios/huellas bajaron de 31→30 y 71→68 confirmando que el PIN 203 (Jose Martinez Ledezma) se borró físicamente del dispositivo; Apex quedó con el comando en `confirmado` y `checador_pin` liberado a `NULL` automáticamente. **Pendiente:** instalar el script como servicio systemd (archivo `checador-listener.service` ya entregado) para que sobreviva reinicios de la Pi — hoy solo se probó en primer plano; probar el flujo de "alta" (crear usuario nuevo) en la próxima sesión, todavía no se ha probado, solo baja. |

| UPD-575 | 08-sep-2026 | Armando | **Mapeo PIN del reloj ↔ ID de dispersión de nómina — campo nuevo `numero_dispersion`, sin tocar el reloj físico.** Armando pasó el listado real de dispersión del despacho de outsourcing (14 personas, ID de 4 dígitos tipo "0017") y preguntó si se podía cambiar el PIN del reloj para que coincidiera con ese ID. Se le explicó el riesgo real (en estos relojes biométricos, reasignar el PIN de alguien ya enrolado normalmente exige borrar y recrear el usuario, lo que borra la huella junto con él — mismo problema ya documentado para "alta" en UPD-567/570) y se recomendó en su lugar traducir los dos números dentro de Apex sin tocar el dispositivo. Armando aceptó la recomendación. **Cruce por nombre (13 de 14 coincidieron con la lista de 29 del reloj de UPD-571; confirmado por Armando):** Beatriz Rocha Briones (0017→PIN 102), Lina Rocha (0031→104), Yahaira Flores (0035→24), Jose Gonzalez Lopez (0093→208), Angel Baltazar (0172→202), Jorge Segundo Rojas (0198→210), Kevin Alexis (0223→23), Jordi Reyes Ovalle (0229→209), Javier Reyes (0230→5), Juan Garcia Sanjuan (0233→206), Victor Bautista (0360→11), Mario Cesar Araujo Martinez (0363→13), Manuel Cruz (0371→20). **Hallazgo de paso:** Jose Martinez Ledezma (PIN 203, dado de baja hoy mismo en UPD-574) confirmado por Armando que efectivamente ya no trabaja en la empresa — no aparece en la dispersión, validando que fue una baja real y no solo una prueba arbitraria. **Caso especial: Jesús Hernández Guerrero (0239)** no aparecía en la lista de 29 porque trabaja en otra ubicación (no usa este reloj) — se da de alta en RH/Nómina de Apex para tener su expediente, pero **sin `checador_pin`** (no aplica, no checa en este dispositivo). Checkpoint: tag git `pre-numero-dispersion-20260908` + backup `_backups/pre_numero_dispersion_nomina_empleados_20260908.sql`. **BD:** `nomina_empleados` +columna `numero_dispersion` (VARCHAR(10), UNIQUE, NULL — se guarda como texto para conservar los ceros a la izquierda). Actualizados Angel Baltazar y Lina Rocha (ya existentes desde UPD-571) con su `numero_dispersion`; insertados 10 empleados nuevos confirmados (los de la tabla de arriba, activos, área planta salvo Lina que es oficina) + Jesús Hernández Guerrero (activo, sin PIN). Verificado con SELECT antes (0 choques de PIN ni de `numero_dispersion`) y después del INSERT/UPDATE — sin dry-run esta vez, autorizado explícitamente por Armando antes de ejecutar. **Decisión para más adelante, documentada, sin ejecutar hoy:** el resto de la lista de 29 que NO aparece en esta dispersión (15 personas: Juan Montelongo, Bernardo Leal Lara, Cynthia Negrete Carrillo, Humberto Francisco, Junol, Jose Luis, Antonio Monsivais, Irene Medellin, Ramses Juarez, Francisco Vazquez, Kevin Saucedo, Oscar Cantu, Gilberto, Daniel Monsivais, Isaac Morales) son bajas reales según Armando — si en el futuro hace falta cargarlos en Apex por cualquier motivo (historial, trazabilidad), deben entrar directo con `activo=0`, nunca como activos. **Corrección aplicada de inmediato (mismo UPD, detectada por Armando al revisar):** el INSERT original se saltó a Beatriz Rocha Briones por error de transcripción (estaba en la tabla de cruce pero no en el INSERT ejecutado) — agregada (id=20, PIN 102, dispersión 0017); además Angel Baltazar y Lina Rocha quedaron con el nombre corto que traía el reloj en vez del nombre completo del despacho — corregidos a "Angel Agustin Baltazar Hernandez" y "Lina Narayana Rocha Gutierrez". Verificado con SELECT final: 14 registros totales (13 activos + Jose Martinez Ledezma de baja), todos con nombre completo. |

| UPD-576 | 08-sep-2026 | Armando | **Nuevo módulo "Asistencia (Checador)" en RH — captura real de checadas (ATTLOG) del reloj hacia Apex + página de reporte semanal.** Cierra el hueco que quedaba desde UPD-574 ("hoy solo se registra en el log, no se manda a Apex"). Armando pidió ver entradas/salidas ordenadas por `numero_dispersion` (UPD-575), con semana real Jueves-Miércoles pero encabezado mostrando el miércoles anterior como referencia visual ("de miércoles a miércoles") — confirmado con fechas reales: semana 35 = real 27ago-2sep, header "26 ago – 2 sep"; semana 36 = real 3-9sep, header "2 sep – 9 sep", coincide exacto con lo que describió. Mostrando **todas** las checadas del día (no solo primera/última), igual que el reporte que ya sacaba manualmente del reloj. **BD:** tabla nueva `checador_checadas` (`pin`, `fecha_hora`, `estatus`, `verify_mode`, único por `(pin, fecha_hora)` para deduplicar reenvíos). **`api/checador.php`:** función `checadorSemanaJueMier()` (semana real jueves-miércoles, distinta de la lunes-domingo de Bono de Corte); nueva acción de dispositivo `registrar_checadas` (POST, misma llave `X-Checador-Key`) que recibe líneas crudas de ATTLOG y las inserta deduplicadas; nueva acción de UI `asistencia_semana` (GET, `ver_rh`) que arma la tabla completa por empleado activo × día, ordenada por `numero_dispersion`. **`checador_listener.py` (Pi):** `_handle_cdata_post` ahora reenvía las líneas de ATTLOG a Apex en un hilo aparte (fire-and-forget, no bloquea la respuesta al reloj) vía la función nueva `reenviar_checadas()`. **De paso, bug de infraestructura encontrado y corregido:** al reiniciar el servicio manualmente (`stop`+`start` en la misma sesión SSH) tronaba con `OSError: Address already in use` porque el proceso anterior tarda unos segundos en soltar el puerto 8081 — se agregó `allow_reuse_address = True` al servidor; en la práctica el `Restart=always` de systemd ya lo resolvía solo en ~10-15s de reintentos, pero con el fix debería converger más rápido. **Nuevo módulo `app/modulos/rh_asistencia.php`** (sidebar "Asistencia (Checador)" junto a Recursos Humanos, mismo permiso `ver_rh`) — navegación por semana con `‹ ›` (mismo patrón visual que Nómina/Bono de Corte), tabla con columnas Disp./Nombre + 7 columnas de día (etiqueta corta + fecha), celda muestra todas las horas del día separadas por espacio o "—" si no hay checadas; domingo resaltado en gris tenue como recordatorio de que es descanso. **Probado de punta a punta con Playwright MCP (navegador real, no dry-run):** usuario temporal `desarrollo_claude_tmp` (rol `desarrollo`, borrado al terminar) + checadas de prueba reales para 5 empleados en 3 días distintos de la semana — la tabla renderizó exactamente los horarios y días correctos, ordenada por dispersión, con "(sin PIN)" mostrado correctamente para Jesús Hernández Guerrero; navegación a la semana anterior confirmó "26 ago – 2 sep" tal cual. Limpieza total verificada (0 checadas y 0 usuarios de prueba en producción). `php -l` + `node --check` en los 3 archivos, y `py_compile` del listener, todo OK. **Pendiente:** confirmar con datos 100% reales (nadie ha checado todavía desde que se desplegó este reenvío) — se sabrá que funciona en producción cuando aparezca la primera checada real de mañana en la tabla sin intervención manual. **Pendiente para mañana (a petición de Armando, sin tocar hoy):** el listener manda `Stamp=9999`/`OpStamp=9999` fijos en el handshake, lo cual probablemente le indica al reloj "ya tengo todo" y por eso no reenvía su historial ya guardado (2+ semanas) — hipótesis sin confirmar, coincide con que en UPD-567 el listener de prueba (que no mandaba ese Stamp alto) sí recibió el backlog completo de OPERLOG desde dic-2025. Prueba propuesta: bajar `Stamp`/`OpStamp` a `0` temporalmente y reiniciar el servicio para ver si el reloj reenvía su historial completo de ATTLOG — sin riesgo real (no borra ni modifica nada en el dispositivo, la tabla ya dedupe por `(pin, fecha_hora)`). |

| UPD-577 | 10-sep-2026 | Armando | **Corrección de datos (no código): S-639 (orden id=902, COT-1374, MERARI JUAREZ) — partidas 1 y 2 (piezas 6290/6291, Espejo Plata 6mm 598×2125mm y 598×2127mm) no se cargaron/entregaron físicamente pese a que el chofer (Juan Roberto García) confirmó por QR TODA la parada de la ruta (147) como entregada el 07-sep-2026 — solo las partidas 3, 4 y 5 sí salieron. Investigado el modelo de datos: `piezas.estatus='entregado'` y `ordenes.estado='entregada'`/`fecha_cierre` se fijan al registrar la salida en Cobranza (`api/salidas.php`, tipo `chofer`) — es el cierre financiero/de producción, NO la confirmación de entrega física; esa vive aparte en `ruta_entrega_piezas`/`ruta_entregas` (confirmada por el chofer vía QR). Por eso el fix fue exclusivamente en la capa de logística, sin tocar Cobranza: `ruta_entrega_piezas` id 1534/1535 (piezas 6290/6291 de la parada 372, ruta 147) pasadas de `entregada` a `rechazada` (mismo mecanismo que ya usa el sistema para "el cliente no se quedó con estas piezas en una entrega parcial en puerta", UPD-399) — con eso `api/rutas.php` (`accion=pendientes` y `calcularPesoPendienteRutear()`) las vuelve a considerar libres para re-rutear automáticamente, sin necesidad de tocar `piezas.estatus`, `ordenes.estado/fecha_cierre/requiere_ruta` (ya estaba en 1) ni `orden_salidas/orden_salida_piezas` (quedan igual, son el registro histórico de Cobranza). Verificado antes/después con SELECT dentro de la misma transacción del UPDATE (no fue dry-run con rollback — cambio autorizado y ejecutado directo por ser de bajo riesgo/fácilmente reversible) + bitácora nueva en `correcciones_log` (tipo=orden, campo=`ruta_entrega_pieza_rechazada`). Confirmado tras el cambio que S-639 ya aparece en Logística Rutas → Pendientes de asignar, solo con el peso de las partidas 1 y 2 (query de verificación replicando exactamente el criterio de `accion=pendientes`). Sin cambios de código — 100% corrección de datos. |

| UPD-578 | 10-sep-2026 | Armando | **Reporte Dirección → "Rendimiento por asesor": columna "Cotizado (pipeline)" dejó de estar hardcodeada a Bethy/Berenice + columna nueva "% Conversión" por asesor.** Armando preguntó por qué en esa tabla solo aparecía pipeline para Bethy (Cynthia salía con "—") y luego, más notorio, por qué Yahaira Flores (asesora #1 en ventas del mes, $237,934.30) tampoco mostraba nada — investigado: `api/reporte_direccion.php` calculaba `bethy_total`/`berenice_total` con `SUM(CASE WHEN asesor_nombre LIKE '%Bethy%'...)`/`'%Berenice%'`, dos columnas fijas desde que el módulo se construyó (cuando solo ellas dos cotizaban) que nunca se generalizaron al agregar más asesoras — cualquier otra (Cynthia, Yahaira, Director Administrativo) caía siempre en "—" sin importar cuánto vendiera. **Fix:** nueva query `pipeline_por_asesor` agrupada por `c.asesor_nombre` (mismo filtro que ya tenía `pipeline_total_periodo`: `estatus='cotizacion'`, `folio >= 'COT-0100'`, `created_at` en el período) devolviendo `count` + `total` por asesor; `bethy_total`/`berenice_total` se dejan en la respuesta sin tocar por si algo más los usa, pero el frontend ya no los consume. Pedido de seguimiento de Armando en la misma sesión ("el número de cotizaciones que confirman el pipeline y el % de conversión"): la celda de pipeline ahora muestra monto + "(N cot.)"; columna nueva "% Conversión" con nueva query `conversion_por_asesor` (mismo criterio que la tarjeta global "Tasa conversión" ya existente — `estatus != 'cancelada'`, `orden_id IS NOT NULL` = convertida — pero agrupada por asesor), con el mismo semáforo verde/ámbar/rojo (≥60%/≥40%/<40%) y el detalle "(convertidas/total)". `app/modulos/reporte_direccion.php` consume ambos mapas nuevos (`pipeline_por_asesor`, `conversion_por_asesor`) por `asesor_nombre` exacto — confirmado que Bethy/Berenice no tienen variantes de nombre en `cotizaciones` (`LIKE` vs. exacto da el mismo resultado). Verificado contra datos reales de producción en solo lectura (mes actual): Yahaira Flores $395,715.38 pipeline (55 cot.), 36% conversión (31/86); Bethy $281,573.08 (48 cot.), 45% (39/87); Director Administrativo $52,420.22 (3 cot.), 40% (2/5). `php -l` + `node --check` del `<script>` embebido en ambos archivos, OK. Checkpoint: tag git `pre-pipeline-asesor-generico-20260910`. Sin prueba visual en navegador (no hay Chrome DevTools/Playwright activo en esta sesión) — pendiente que Armando lo confirme viéndolo en vivo. |

| UPD-579 | 10-sep-2026 | Armando | **Fix de UPD-578 (misma sesión): "% Conversión" por asesor en Reporte Dirección inflaba el número con ventas todavía no confirmadas.** Armando reportó que en la fila de Bethy no le cuadraban los números (47 Órdenes vs 39 Convertidas/87). Investigado con SELECTs de solo lectura: son dos cohortes distintas por diseño (Órdenes/Ventas cuenta por fecha de VoBo; Convertidas de UPD-578 contaba por fecha de creación de la cotización) — eso nunca va a igualar 1:1 y ya se le explicó/documentó (mismo patrón que Pipeline vs Ventas, ya existente en este reporte). Pero además del desfase de cohortes, se encontró una contaminación real en el numerador de "convertidas": contaba CUALQUIER cotización con `orden_id` asignado sin importar el estado de esa orden ni si era retrabajo — de los 39 de Bethy, 2 eran órdenes que ya existen pero siguen `pendiente_vobo` (venta aún no confirmada) y 1 era retrabajo (excluido de Ventas/Cobranza en todo el resto del reporte desde UPD-510/512/541, pero esta columna nueva no lo filtraba). Fix en `api/reporte_direccion.php` (`conversion_por_asesor`): `LEFT JOIN ordenes o` + numerador ahora exige `o.estado IN ('activa','entregada')` (igual criterio que Órdenes/Ventas) y denominador (`total_cots`) agrega `es_retrabajo = 0` para no penalizar al asesor con cotizaciones que estructuralmente nunca van a poder contar como "convertidas" bajo el nuevo criterio. `app/modulos/reporte_direccion.php`: nota aclaratoria agregada arriba de la tabla ("Cotizado y % Conversión se miden por fecha en que se creó la cotización; Órdenes/Ventas por fecha de VoBo — no tienen por qué coincidir") para que la próxima persona que vea la tabla no se confunda con el mismo desfase de cohortes. Verificado contra datos reales de producción (mes actual): Bethy pasó de 87/39/45% a **86/36/42%**; Yahaira Flores de 86/31/36% a **86/30/35%**; Director Administrativo sin cambio (5/2/40%, no tenía retrabajo ni pendientes de VoBo en el período). `php -l` + `node --check` OK. Mismo checkpoint de UPD-578 (`pre-pipeline-asesor-generico-20260910`) cubre este ajuste — no se creó uno nuevo por ser correcciones seguidas dentro de la misma sesión sin haber llegado a producción en el estado intermedio. |

| UPD-580 | 10-sep-2026 | Mando | **Checador — limpieza de ex empleados en el reloj físico (14 bajas remotas) + rotación de `CHECADOR_LISTENER_KEY`.** Visita imprevista a oficina aprovechada para cerrar pendientes que exigían presencia física. **(1) Llave rotada** (era una de las 5 filtradas al historial de git, ver auditoría 09-sep): valor nuevo en `.env` del VPS y en `/etc/checador.env` de la Pi (root:root 600, conectado vía `EnvironmentFile` en el servicio — antes vivía hardcodeada dentro de `checador_listener.py`). Verificado end-to-end con checada real: reloj 15:00:56 → servidor 15:01:14. Servidor acepta llave nueva (200) y rechaza cualquier otra (401). Además `systemctl enable` del servicio, que estaba pendiente desde UPD-574. **(2) 14 bajas encoladas y confirmadas por el reloj en <2 min, sin tocar el aparato** — PINs 2,3,4,6,10,12,14,15,18,19,22,103,205,213 (los 15 documentados como bajas reales en UPD-575, menos Irene Medellín). Mando sacó la lista de enrolados del menú del reloj y confirmó uno por uno. Se corrieron 2 candados antes del INSERT: ningún PIN en la lista protegida (13 activos + admin), ninguno ligado a empleado vigente. Se encoló con `empleado_id=0` — verificado que `accion=confirmar` no falla si el empleado no existe (el UPDATE de liberación afecta 0 filas). Conservados a propósito: PIN 1 (cuenta admin del aparato), PIN 8 Irene Medellín (sin certeza de su situación, borrar huella es irreversible; NO existe en `nomina_empleados`, así que si checa su registro no se liga a nadie — pendiente decidir), y Jesús Hernández Guerrero (activo en Apex, sin PIN, trabaja en otra ubicación). Estado final: 13 activos con PIN intactos, 0 registros de asistencia alterados. **Primera operación masiva remota sobre el dispositivo** — confirma que el mecanismo de UPD-570/574 sirve para administración real. **(3) Secretos tachados de documentación trackeada:** `WA_VERIFY_TOKEN` (5 archivos incl. `CLAUDE.md`), `PROTRACK_ACCOUNT` e IMEIs (histórico de UPDs) — 16 ocurrencias, sin borrar ninguna entrada, originales respaldados en `/root/`. De paso se corrigió `scripts/git_guard.sh` para inspeccionar solo líneas agregadas: antes bloqueaba la propia limpieza, porque veía el secreto en las líneas eliminadas. **Auditoría de seguridad completa en `/home/mando/files_apexglass/auditoria_seguridad_2026-09-09/`** (14 documentos, 9 fases + red de oficina, registro de riesgos priorizado). Pendiente: rotar los 2 tokens de WhatsApp (instrucciones en `05d_instrucciones_whatsapp.md`, Mando ya tiene acceso admin a Meta) y conseguir no-break para la Pi. |

